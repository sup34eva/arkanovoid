extends layout

block styles
	link(href="/css/game.css", rel="stylesheet")

block content
	#listener.row.well
		embed(width="1100" height="700" src="/server/server.nmf" type="application/x-nacl")

block scripts
	script.
		var container = document.getElementById('listener');
		container.addEventListener('message', function(e){console.log(e.data);}, true);
