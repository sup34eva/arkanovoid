coffeescript:
  - 'coffee'
  - 'uglify'

server:
  - 'express'
  - 'open'
  - 'watch'

make:
 - 'shell:make'
 - 'copy:server'

build:
  - 'concurrent:build'

test:
  - 'concurrent:test'

commit:
  - 'test'
  - 'make'
  - 'bump'

default:
  - 'build'
  - 'server'
